apiVersion: v1
kind: Service
metadata:
  name: {{ include "k8webapp.fullname" . }}
  labels:
    {{- include "k8webapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "k8webapp.selectorLabels" . | nindent 4 }}

version: "3.9"  # optional since v1.27.0
services:
  web:
    build: 
      context: .
      dockerfile: Dockerfile
    image: python_docker_k8_app:2.0
    ports:
      - "5020:5000"
    restart: always
    networks: 
     - webnet
     
networks:
  webnet: